registrator:
  image: gliderlabs/registrator
  restart: always
  net: "host"
  volumes:
    - "/var/run/docker.sock:/tmp/docker.sock"
  command: "-ip <%= @ipaddress_eth1 %> consul://<%= @consul_ip %>:8500"
